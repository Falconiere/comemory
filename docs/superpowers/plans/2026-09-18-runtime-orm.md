# Runtime SQL to toolu-orm implementation plan

> **For agentic workers:** Use superpowers:executing-plans for the shared
> execution bridge and superpowers:dispatching-parallel-agents for disjoint
> store families. Track completion in the checklist below.

**Goal:** Replace handwritten runtime queries with the existing toolu-orm
schema builders wherever they preserve behavior; report missing capabilities
to Falconiere/toolu-orm and document every remaining production SQL family.

**Architecture:** Keep the synchronous store connection and caller-owned
transactions. Generate SQL and bound values using the declared schema, then
execute through a small store-private bridge that preserves owned results,
row decoding, affected-row counts, and SQLite error classification.

**Tech stack:** Rust, toolu-orm 0.6.0, rusqlite 0.40, SQLite FTS5/sqlite-vec.

**Spec:** The user request in this session and the binding rules in AGENTS.md.

## Constraints

- Preserve result ordering, pagination, null handling, conflict semantics,
  transaction boundaries, and query performance characteristics.
- Do not substitute INSERT OR REPLACE for ON CONFLICT DO UPDATE: replacement
  deletes rows and can cascade into dependent data.
- Keep the driver confined to store/. No driver cursors escape the store.
- Keep shipped migration SQL byte-identical; it is the migration journal's
  historical payload. Test fixture SQL independently verifies the live schema.
- Do not hide unsupported SQL inside a new raw-query wrapper or reproduce an
  ORM inside this repository. Retain and link genuinely unsupported cases.
- Files stay below 300 code lines and functions below 100 lines.

## Tasks

- [x] Inventory runtime SQL throughout the repository, compare toolu-orm's
  installed implementation and upstream issues, and classify unsupported forms.
- [x] Add `src/store/orm.rs`, declared privately in `src/store.rs`. It accepts
  `(String, Vec<toolu_orm::core::value::Value>)` generated by `.to_sql()` and
  exposes `execute -> Result<usize>`, `query_all -> Result<Vec<T>>`,
  `query_one -> Result<T>`, and `query_optional -> Result<Option<T>>`, with
  the existing row-mapping closures. Prove bound values, missing rows, native
  SQLite errors and caller transaction rollback against an in-memory database.
- [x] Convert code-index CRUD, metadata and history in their existing
  `src/store/{code_*,indexed_files,index_*,repo_*}.rs` owners. Preserve joins,
  aggregate/subquery behavior and custom upserts until the ORM supports them.
  Run the corresponding colocated nextest suites.
- [x] Convert learning and sync CRUD in `src/store/{bandit_arms,feedback,
  query_expansions,retrieval_log,eval_runs,gc_*,sync_*}.rs`. Keep timestamp
  comparisons and counter updates semantically identical. Run their suites.
- [x] Convert memories, documents, graph, FTS/vector operations and maintenance
  probes where supported. Audit remaining statements in mixed files, including
  migration bookkeeping and rebuild-copy lifecycle helpers. Run their suites.
- [x] File missing upstream issues with original SQL examples and exact API
  limitations; link existing issues when they already cover the gap. Add a
  repository inventory document mapping retained SQL to those issues.
- [x] Update the store folder index and architecture/dependency descriptions.
  Verify schema snapshots and migration payloads remain unchanged.
- [x] Run `cargo fmt --all`, `cargo nextest run --all-features`, and
  `bash scripts/check-all.sh`; inspect the final diff and raw-SQL inventory.

## Conversion example

```rust
use toolu_orm::core::query_column::CommonOps;
use super::{orm, schema_code::{IndexedFiles, indexed_files}};

orm::execute(conn, IndexedFiles::delete()
    .filter(indexed_files::repo.eq(repo)).to_sql())?;
let query = IndexedFiles::select()
    .columns_typed(&[&indexed_files::blob_oid])
    .filter(indexed_files::repo.eq(repo))
    .filter(indexed_files::path.eq(path));
orm::query_optional(conn, query.to_sql(), |row| row.get(0))
```

## Review and validation notes

- Final validation: 2,459 tests passed across 121 binaries; every
  `bash scripts/check-all.sh` gate passed. `git diff --check` is clean.
- Upstream gaps are filed as Falconiere/toolu-orm #108–#115 and mapped in
  `docs/guides/runtime-orm.md`. Shipped migrations and Cargo.lock are unchanged.
- Independent query review caught fractional BM25 weight rounding; a real
  SQLite regression failed before the correction and passed afterward.
- The setup hook test now selects only `git-hooks`, avoiding a pre-existing
  dependence on the developer's registered Codex marketplace. The original
  handwritten-hook preservation assertion remains unchanged.
- The additional duplication scan still fails the existing baseline of 113.
  With the same similarity-rs 0.5.0 invocation, HEAD has 242 pairs and this
  change has 278, including new files normally omitted by `git ls-files`.
  Review of the 47 added and 11 removed pairs identified shared row execution
  and metadata ID binding, which were extracted. Remaining matches reflect
  typed CRUD chains and thin wrappers, including read-versus-write pairs.
  `docs/dup-debt.md` documents the pre-existing baseline problem; the baseline
  is unchanged. Comparison artifacts are in
  `/tmp/comemory-duplicate-review-m9uj6yej/final_comparison.md` for this session.

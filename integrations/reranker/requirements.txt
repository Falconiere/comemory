# Pinned dependency set for the cross-encoder scoring mode only.
#
# The deterministic `--scoring lexical-overlap` mode, the warm `client` and
# `--help` need none of this: they are Python standard library alone, which is
# what keeps comemory's normal `cargo nextest` path free of every package
# below.
#
# Verified against PyPI on 2026-09-18. peft 0.21.0 (2026-09-15) postdates
# transformers 5.17.0 (2026-09-09), so it is the first PEFT release built
# against that Transformers major. transformers 5.17.0 requires
# huggingface-hub <2.0,>=1.5.0, tokenizers <0.24.0,>=0.23.1 and
# safetensors >=0.8.0 — all satisfied here. numpy 2.5.3 requires Python >=3.12,
# which is therefore the floor for this mode.
#
# These are exact pins of the DIRECT dependency set. The transitive closure is
# left to pip rather than enumerated, because enumerating it without resolving
# it in the target environment would record guesses as pins. Keep every version
# identical to REQUIREMENTS in comemory_rerank_pins.py: run-model-tests.sh
# refuses to run the opt-in suite against anything else.
torch==2.14.0
transformers==5.17.0
peft==0.21.0
tokenizers==0.23.2
safetensors==0.8.0
huggingface-hub==1.32.0
numpy==2.5.3

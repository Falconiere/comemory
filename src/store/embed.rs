//! `f32` ↔ `sqlite-vec` BLOB encoding plus dim-guard helpers.
//!
//! Skeleton committed by Task 2 of the v0.2 plan so downstream tasks
//! can import `crate::store::embed`. Task 5 fills in `to_vec_blob`,
//! `from_vec_blob`, and `guard_dim`.

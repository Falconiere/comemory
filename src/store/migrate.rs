//! Versioned schema migrations tracked via the `schema_meta` table.
//!
//! Skeleton committed by Task 2 of the v0.2 plan so downstream tasks
//! can import `crate::store::migrate`. Task 3 fills in `CURRENT_VERSION`
//! and `run`, plus the bundled SQL files under `src/store/sql/`.

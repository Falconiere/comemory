/target
/web/node_modules
/web/.vite
/.claude/tmp/
/.fastembed_cache/

# Local MCP config — not committed
.mcp.json

# claudness workflow artifacts (specs/plans) — local, not committed
/docs/claudness/
